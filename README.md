# My scroll build

## Installation
```
git clone https://github.com/DefinitelyNotMai/scroll
cd scroll
sudo make install
```

## Configuration
Configure scroll by making changes in "config.h", run "sudo make install" afterwards.
